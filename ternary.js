// ternary operator is a conditional operator

// if product  adds a product  display a added  product
// if product  not added a product  display a not added

product = -1;
let cart = product > 0 ? "addedproduct" : "not added";
console.log(cart);
